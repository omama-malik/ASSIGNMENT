const famous_person = 'Albert Einstein';
const quotes = 'A person who never made a mistake never tried anything new.';
const messages = `${famous_person} once said, "${quotes}"`;

console.log(messages);
